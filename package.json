{
  "name": "express",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "installAll": "npm install && npm run installBlog",
    "deploy": "npm run buildBlog && npm run push",
    "start": "npm run buildBlog && node index.js",

    "installBlog": "npm install --prefix Blog",
    "buildBlog": "npm run build --prefix Blog",

    "push": "git add . && git commit -m '1' && git push -f origin main",
    "pull": "git stash && git pull -f origin main && git stash clear"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.21.2"
  }
}