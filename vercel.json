{
    "version": 2,
    "builds": [
        {
            "src": "index.js",
            "use": "@now/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.js"
        }
    ],
    
    "devCommand": "npm run installBlog && npm run buildBlog",
    "scripts": {
        "build": "npm run installBlog && npm run buildBlog"
    }
}