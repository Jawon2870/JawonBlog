{
    "version": 2,

    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.js"
        }
    ],
    
    "installCommand": "npm run installBlog && npm run buildBlog && node index.js"
}