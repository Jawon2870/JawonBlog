{
    "version": 2,

    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.js"
        }
    ],
    
    "buildCommand": "npm run installBlog && npm run buildBlog && node index.js"
}